# Grafana dashboard provisioning configuration
apiVersion: 1

providers:
  - name: 'encrypted-email-dashboards'
    orgId: 1
    folder: 'Encrypted Email Service'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json